<h1>All Notes</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Note</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <%@notes.each do |note| %>
      <tr>
        <td><%= note.title %></td>
        <td><%= note.content%></td>
       </tr>
       <tr>
        <td><%= link_to 'View note', note %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<%= link_to 'Add note', new_note_path %>